kind: Fixed
body: 'Fixed a refresh storm where a multi-step git operation (e.g. `gs stack submit`) triggered continuous stack-view refreshes for the whole command. Watcher-driven refreshes are now held while the operation''s git markers (index lock, rebase/merge state) are present and fire once they clear, collapsing the burst into a single trailing refresh with no fixed delay.'
