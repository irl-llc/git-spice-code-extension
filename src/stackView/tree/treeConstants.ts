/** Tree visualization constants matching VSCode SCM history style. */

/** Width of each swim lane in pixels. */
export const LANE_WIDTH = 11;

/** Circle radius for non-current branches. */
export const NODE_RADIUS = 4;

/** Circle radius for current branch (larger). */
export const NODE_RADIUS_CURRENT = 5;

/** Stroke width for branch node circles. */
export const NODE_STROKE = 2;

/** Radius for rounded corners on path curves. */
export const CURVE_RADIUS = 5;

/** Gap between line endpoints and node edge (halo effect). */
export const NODE_GAP = 7;
